@model Inisra_Web_App_MVC.Models.Invitation

@{
    ViewBag.Title = "Invite";
}

<h2>Invite</h2>

<div>
    <h4>Invitation</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Job Title
        </dt>

        <dd>
            @Html.ActionLink(Model.Job.Title, "Details", "Jobs",  new { id = Model.JobID }, new { })
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.JobSeeker.FullName)
        </dt>

        <dd>
            @Html.ActionLink(Model.JobSeeker.FullName, "Details", "JobSeekers", new { id = Model.JobSeekerID }, new {} )
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.InvitationDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.InvitationDate)
        </dd>

    </dl>
</div>
<p>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <input type="submit" class="btn btn-default" value="Invite" />
    }
    @Html.ActionLink("Back to List", "Index")
</p>
